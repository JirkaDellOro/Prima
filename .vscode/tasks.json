{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "serve",
            "type": "shell",
            "command": "serve",
            "isBackground": true
        },
        {
            "label": "auto-watch",
            "type": "shell",
            "command": "tsc -watch",
            "runOptions": {
                "runOn": "default"
            }
        }
    ]
}